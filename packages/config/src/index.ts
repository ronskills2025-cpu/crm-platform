/**
 * @packages/config — Centralized environment configuration
 *
 * Single source of truth for all env vars and config values.
 * Modules import { config } from '@packages/config'.
 */

export { config } from './config';
